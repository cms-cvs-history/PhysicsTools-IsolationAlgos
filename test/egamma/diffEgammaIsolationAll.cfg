process diffEgamma  = {
    include "FWCore/MessageService/data/MessageLogger.cfi"
    source = PoolSource {
        untracked vstring fileNames = {  'file:out.root' 
        }
    }
    include "PhysicsTools/IsolationAlgos/test/egamma/diff.cff"
    path all = {        diff      }
    service = TFileService { string fileName = 'plots.root' }
    untracked PSet maxEvents = { untracked int32 input = 100 }
}
