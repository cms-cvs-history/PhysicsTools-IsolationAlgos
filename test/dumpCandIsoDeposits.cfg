process dumpDep = {
    include "FWCore/MessageService/data/MessageLogger.cfi"
    source = PoolSource {
        untracked vstring fileNames = {  'file:mu/out.root' 
        }
    }
    module test = CandIsoDumper {
        InputTag src = muIsoDepositCtfTk
    }
    path p = { test }
    untracked PSet maxEvents = { untracked int32 input = -1 }
}
